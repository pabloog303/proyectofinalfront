services:
  - type: web
    name: ai-fitness-coach
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run dev:server
    envVars:
      - key: PORT
        value: 8787
      - key: JWT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
